name = "checkemaildelivery-email-worker"
main = "email-worker.js"
compatibility_date = "2024-01-01"

# No [vars] here — set BACKEND_URL and WORKER_SECRET
# in the Cloudflare dashboard under Worker Settings → Variables
# so they stay out of source control.
